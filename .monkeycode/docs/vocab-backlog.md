# 背词待办记录

## 已完成

- v1.0.38 批次：法→中长按显词、题序随机、中途退出续学、进度云同步、收藏显示修复、口语入口隐藏换成「日历和数字」页。
- v1.0.39 批次：背词中文释义全面清洗（义项分层择优、剔除变位元语言条目、清除中西混排残串、片段边界截断），词书 20686 条。

## 仍未完成

- **法语词条本身的残缺**：源词典 `dictionary.db` 中混有个别词形损坏的记录（如 `lnteraction` 应为 `interaction`，首字母 I 被 OCR 成 l；`zézayer` 释义源数据残缺）。规模很小，但会在背词卡里露出。修复需在 `tools/build_vocab_assets.py` 里加词形校正表或改 `build_dict_db.py` 源清洗。
- **多义词首义偏窄**：当前只取首个合格义项（最多补一段）。更贴近常用义需要语义层面的词频/义项排序数据，离线词典不具备；如需进一步提升，可引入外部词频-义项对照表离线入库。
- **裸 `n.` 无语法性别**：词典中仅 3 条有可补的 n.m./n.f. 对照，覆盖率太低，暂不处理；影响语法练习的冠词生成器的可选词范围。
