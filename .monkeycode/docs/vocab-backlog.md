# 背词待办记录

v1.0.38 批次已完成：法→中长按显词、题序随机、中途退出续学、进度云同步、收藏显示修复、口语入口隐藏换成「日历和数字」页。

## 仍未完成

- **中文释义全面排查**（用户反馈背单词/背动词部分词义不准）：需对 `assets/vocab/vocab.json` 全量 27k+ 词条做语义审查，重点：多义词只取首义导致偏、pos 标记 "n." 不带性（9928 条）、机翻痕迹。修复通道为 `tools/build_vocab_assets.py` 重新生成后须重跑词书资产构建与发包流程。
